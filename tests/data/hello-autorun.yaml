name: hello-autorun
machine: pet4032
program: ../../demos/hello-basic/program.bas
timeout: 30
steps:
  - wait: { text: "HELLO FROM BASIC" }
  - wait: { text: "2+2= 4" }
  - assert: { screen: "READY." }
  - assert: { reg: pc, in_range: ["$0000", "$FFFF"] }
